// examples/demo.ltl — runnable demo for ltl_syslog_send

import std::io
use ltl_syslog_send as lib

fn main() {
    io::println("ltl_syslog_send demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}