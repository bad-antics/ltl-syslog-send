// ltl-syslog-send/main.ltl
//
// RFC 5424 syslog UDP / TCP emitter with structured data.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-syslog-send v0.1.0")
    io::println("RFC 5424 syslog UDP / TCP emitter with structured data.")
    0
}
